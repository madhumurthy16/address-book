// TDD a feature that will return the number of contacts in our book. getContactCount()

const MenuController = require("../controllers/MenuController");

//Test suite name "MenuController"
describe("MenuController", () => { 
	beforeEach(() => {
		this.menu = new MenuController();
	});
});